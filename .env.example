# 复制为 .env 后按需修改

# Claude VLM（用户语音提问时需要）
ANTHROPIC_API_KEY=

# ── NitroGen ─────────────────────────────────────────────────────
# 仅测前端闭环（默认）：模拟 perception，无需 GPU/ZMQ
NITROGEN_MOCK=1

# 接上真实 NitroGen 推理服务时改为 0，并填写地址：
# NITROGEN_MOCK=0
# NITROGEN_SERVER=tcp://localhost:5555
